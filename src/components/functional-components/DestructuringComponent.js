import React from 'react'

// instead of props we can write its properties name 
const DestructuringComponent = ({prop1, prop2}) => {
    return (
        <div>
            
        </div>
    )
}
export default DestructuringComponent